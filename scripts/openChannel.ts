import hre from "hardhat";
import openChannel from "./private/_openChannel";

async function main() {}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
